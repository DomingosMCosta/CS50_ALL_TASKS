First install a c compiler, I used https://sourceforge.net/projects/mingw/

Then run gcc -o volume volume.c or directly on the run menu and choose the correct compiler

If volume.exe already exists then simply run (examples) .\volume input.wav output.wav 2.0 or .\volume input.wav output.wav 0.5

This will generate and output file with the changed volume