First install a c compiler, I used https://sourceforge.net/projects/mingw/

Then run gcc -o inheritance inheritance.c or directly on the run menu and choose the correct compiler

If inheritance.exe already exists then simply run .\inheritance.exe